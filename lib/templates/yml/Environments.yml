        
    <%= metadata.project.name.toUpperCase()%>_<%= entity.name.toUpperCase() %>_TABLE_NAME: ${self:custom.<%= metadata.project.name %><%= entity.name[0].toUpperCase() %><%= entity.name.substring(1)%>TableName}
    